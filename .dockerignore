# 의존성 (빌드 중 npm ci 로 재설치)
node_modules/
.npm

# 빌드 산출물 (Docker 내부에서 새로 빌드)
dist/
build/

# 환경 변수 (보안)
# .env.production 은 CI에서 GitHub Secrets로 생성되어 Vite 빌드에 사용됨 — 제외하지 않음
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production.local

# Git
.git
.gitignore
.github

# IDE / 에디터
.idea/
.vscode/
*.swp
*.swo
*~

# 로그
*.log
logs/
npm-debug.log*

# 테스트 커버리지
coverage/

# OS
.DS_Store
Thumbs.db

# 문서 (이미지 불필요)
*.md
!README.md

# Claude
.claude/
